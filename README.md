# SimpLearn
Built a compiler for a custom language (Simplearn) that supports variables, arithmetic, if/else, loops, and I/O — tokenizes, parses, type-checks, and translates code into working C programs using Lex and YACC.
